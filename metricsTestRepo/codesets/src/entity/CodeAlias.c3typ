entity type CodeAlias schema name 'CODEALIAS' {

   /**
      id is the customers raw/original code which is then mapped to alias (canonical meaning)
    */
   id : ~

   measurementCode: !MeasurementCode

   insightTask: string
   
   alias: string

}